import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'NutriMind AI - Smart Nutrition Tracking',
  description: 'AI-powered nutrition tracking app with meal scanning, personalized meal plans, and health insights',
  generator: 'v0.app',
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: 'NutriMind AI',
  },
  icons: {
    icon: [
      { url: '/icons/icon-192.jpg', sizes: '192x192', type: 'image/jpeg' },
      { url: '/icons/icon-512.jpg', sizes: '512x512', type: 'image/jpeg' },
    ],
    apple: '/icons/icon-192.jpg',
  },
}

export const viewport: Viewport = {
  themeColor: '#059669',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
